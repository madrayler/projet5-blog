<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Connexion à l'administration</title>
     <link rel="stylesheet" type="text/css" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
     <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/cropperjs/1.4.1/cropper.min.css">
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
     <link rel="stylesheet" href="{{ asset('build/app.css') }}">
      {#<link rel="icon" type="image/x-icon" href="{{ asset(images/velov.jpg) }}"/>-->#}
  </head>
  <body>
	<div class="container mt-4">

	<h1><i class="fas fa-lock"></i>Connexion à l'administration</h1>
	{% if error %}
		<div class="alert alert-danger">
			{{ error.messagekey | trans(error.messagedata, 'security') }}
		</div>
	{% endif %}
	<form action="{{ path('admin_connect_login') }}" method="post">
		<div class="form-group">
			<input type="text" required name="_username" placeholder="Nom d'utilisateur ..." class="form-control" value="{{ last_username }}">
		</div>
		<div class="form-group">
			<input type="password" required name="_password" placeholder="Mot de passe ..." class="form-control">
		</div>
		<div class="form-group">
			<button type="submit" class="btn btn-success">Connexion</button>
		</div>
	</form>
	</div>
  </body>
 </html>